# Profile "minimal", for a server, a VM, a machine you only pass through.
# The terminal and the editor, nothing else. No graphical session.
# `steps:` drops build-suckless and fonts, which have no purpose without a session.

packages: core shell
system:
steps: install link system-apply nvim-patch
aur: no

shell
git
nvim
vim
