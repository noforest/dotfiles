# Battery, backlight, lid, fingerprint, gestures
acpi
acpid
brightnessctl
power-profiles-daemon
xss-lock
xautolock
fprintd
s-tui
ethtool
solaar
rclone
#
# No equivalent in the archive, see packages/ubuntu/manual.md:
#   acpilight          -> brightnessctl above covers the same need
#   libinput-gestures  -> called by .xinitrc, install by hand
#   xidlehook          -> called by xidlehook-start.sh, install by hand
#   touchegg           -> PPA only, and unused here (libinput-gestures does it)
