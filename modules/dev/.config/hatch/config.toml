mode = "local"
project = ""
shell = ""

[dirs]
project = []
python = "isolated"
# data  = "…"   # à compléter — commenté : hatch reprend son défaut (~/.local/share/hatch)
# cache = "…"   # à compléter — commenté : hatch reprend son défaut (~/.cache/hatch)

[dirs.env]

[projects]

[publish.index]
repo = "main"

[template]
name = ""            # à compléter
email = ""           # à compléter

[template.licenses]
headers = true
default = [
    "MIT",
]

[template.plugins.default]
tests = true
ci = false
src-layout = true

[terminal.styles]
info = "bold"
success = "bold cyan"
error = "bold red"
warning = "bold yellow"
waiting = "bold magenta"
debug = "bold"
spinner = "simpleDotsScrolling"
